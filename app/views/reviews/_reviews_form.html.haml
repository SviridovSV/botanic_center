.col-sm-8
  = form_for(book.reviews.build) do |f|
    = f.hidden_field(:book_id, value: book.id)
    .form-group
      - 5.times do |rate|
        = f.radio_button :rating, "#{rate + 1}"
    .form-group
      %label.control-label.font-weight-light{for: "title"} Title
      = f.text_field :title, placeholder: "Enter title", id: "title", class: "form-control"
    .form-group
      %label.control-label.font-weight-light{for: "content"} Review
      = f.text_area :content, placeholder: "Review", id: "content", class: "form-control", rows: "8"
    .text-right
      = f.button "Post", class: "btn btn-default mb-20"